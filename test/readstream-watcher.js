'use strict';

var test = require('tape');

test('Module exports a function');
test('Function returns an object');
test('Function requires an argument');
test('Function requires a valid filepath');
test('Function returns a ReadStream-compatible object');
test('Function returns a Watcher-compatible object');
test('ReadableWatcher is readable')
test('ReadableWatcher emits "open" event')
test('ReadableWatcher emits "change" event')
